# M2 E2E — sc-1 průchod klikáním v UI (2026-08-14)

Skript: `ui-web/editor-verify.mjs` (plný sc-1 flow, Playwright/Chromium headless; `--skip-paid` re-použije výsledky placených kroků z `scratch/m2-e2e/results.json`).
Prostředí: API uvicorn :8300 + vite :5173 z hlavního checkoutu, master `a457211` (transform-snapshot fix zahrnut až po běhu — viz Nálezy).
Akceptace M2: E2E průchod sc-1 **klikáním v UI**, ne přes curl (zadání M2; ideal-state kritéria #1–#5, #11, #12).

## Výsledky: 13/13 PASS

| # | Krok | Důkaz |
|---|------|-------|
| 1 | seed obrázek (composite decompose-test, 544×736) | e2e-sc1.png |
| 2 | načtení editoru | krok_02 |
| 3 | **dekompozice z UI** (upload → 4 vrstvy, 40,4 s) | krok_03 |
| 4 | **výběr prvku klikem** (alfa hit-test, klik-verifikace přes `__editorState`) | krok_04 |
| 5 | **posun myší** (+76/+38 px, ověřeno v project.json ±2 px) | krok_05 |
| 6 | **scale Transformer kotvou** (544×736 → 568×768) | krok_06 |
| 7 | brush mechanika (2 tahy → `__brushCanvas` pixely + chip v Inspectoru; zdarma) | krok_07 |
| 8 | **AI edit prvku** (qwen-inpaint, 13,3 s, hash PNG vrstvy změněn, vizuálně zlatožlutý recolor) | krok_08 |
| 9 | **inpaint pozadí za prvkem** (12,5 s, hash spodní vrstvy změněn, po posunu prvku vidět vyplněné pozadí) | krok_09 |
| 10 | SAM segment („0 masek" je validní výsledek na této ilustraci; UI ho zobrazí) | krok_10 |
| 11 | **export PNG + parita náhledu** (viz níže) | krok_11, export.png |
| 12 | undo/redo smoke (bez pádu, projekt načtitelný) | results.json |

Placené operace: **4 z max 8** (decompose, ai-edit, inpaint-behind, segment — vše napoprvé, bez retry). Console errors: 0.

## Pixel-diff parita (ideal-state #5: „export PNG odpovídá náhledu")

`scripts/pixel_diff.py` (RGB, worst-channel per pixel):

| Dvojice | mean_abs_diff | max | pct px > 8 |
|---|---|---|---|
| export.png vs GET /composite | **0,0** | 0 | 0 % |
| export.png vs živý Konva stage capture (1/fitScale, bez rámečku/Transformeru) | **0,72** | 42 | 0,97 % |

Server-side cesty jsou identické; klientský náhled se liší jen interpolací při zpětném upscalu capture — bez strukturálních rozdílů. **Kritérium #5 splněno.**

## Nálezy

1. **`/transform` nevolal `_snapshot()`** — jediná mutující route neviditelná pro undo/redo (posun/scale nešly vzít zpět). Opraveno v `a457211` + regresní test `test_transform_snapshots_for_undo`; ověřeno živě na běžícím serveru (undo_count 1→2 po transformu).
2. **Server `/hittest` vs klientský Konva hit-test se rozcházejí**, když layer.width/height ≠ nativní rozměry PNG (server počítá v nativních pixelech, klient renderuje protažené). Založen samostatný task (chip `task_a11cf772`); klientský `drawHitFromCache` je autoritativní pro UI výběr.
3. Kling/AI operace hlásí elapsed v UI; chybové stavy testovány jen offline (66+1 pytest testů, 502 s důvodem) — reálný provider fail během E2E nenastal.

## Artefakty

`scratch/m2-e2e/` (worktree; **negitované, ephemerní**): 16 screenshotů `krok_*.png`, `export.png`, `composite_api.png`, `preview_capture.png`, `results.json` (kompletní kroky/časy/response statusy). Projekt `e2e-sc1.grafik` ponechán v `projects/` hlavního checkoutu jako živý důkaz.
