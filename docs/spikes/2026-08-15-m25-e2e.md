# M2.5 E2E — UX/discoverability blok (2026-08-15)

Klikací ověření v **reálném Chrome** (claude-in-chrome MCP) nad vite:5173 + uvicorn:8300 (oba z hlavního checkoutu). Commity `540f196` (backend), `49d03c1` (frontend), `ccdf5a8` (SAM point fix), `53a880b` (400 fix), `0d14ed2` (uniquify), `049fc6d` (polish). Testy: **119 pytest pass** (12 nových v `tests/test_api_m25.py`).

Placené cally session: **4× SAM-3 (~$0.02)** — probe A+B (scratchpad skript), 1× `/segment` přes API (vrátil 0 masek → odhalil prompt-klíč past), 1× UI klik. Dekompozice dropem reálně NEspuštěna (limit „jen SAM point cally"; drop sdílí `handleCreateFromImage` s file pickerem ověřeným v M2 — wiring ověřen error-cestou níže).

## Kroky a výsledky

| # | Krok | Výsledek |
|---|------|----------|
| 1 | Empty state bez projektu | ✅ dropzone „Začněte obrázkem" + „Přetáhněte obrázek sem…" + primární „Nahrát nový obrázek" + „…nebo vyberte existující projekt vlevo" ([img/…01-empty-state.png](img/2026-08-15-m25-01-empty-state.png)) |
| 2 | Toolbar/panely česky | ✅ Výběr·Štětec·Segmentace·Pohyb, Zpět/Znovu/Vyplnit pozadí/Export PNG, „Rozložit na N vrstvy/vrstev", Inspektor: AI úprava / Vyplnit pozadí / Segmentace textem („popište prvek (např. socha)" + tip na klik-segmentaci), Pohyb panel, statusy vykáním |
| 3 | Drag&drop na plátno | ✅ syntetický dragover → overlay „Pusťte obrázek — rozloží se na 4 vrstvy (nový projekt)"; drop → busy „Dekompozice na 4 vrstvy běží…" → flow createProject→decompose/file |
| 4 | Chybová cesta dropu (poškozený PNG, neplacené) | ✅ po fixu `53a880b` čitelný banner „Nový projekt z obrázku selhal: Soubor nejde přečíst jako obrázek: … (HTTP 400)". **Nález:** před fixem neošetřený 500 obchází CORSMiddleware → prohlížeč vidí jen „Failed to fetch" |
| 5 | Seznam projektů: datumy + řazení | ✅ řazeno dle `updated_at` desc, formát „15. 8. 02:16" (+ rok mimo aktuální rok); po rename/duplicate se pořadí živě přeskládá |
| 6 | First-run hint | ✅ po načtení projektu lišta „Kliknutím na prvek v obraze se vybere vrstva · tažením ji posunete · rohy mění velikost a rotaci", × zavře + `localStorage[grafik.firstRunHint.v1]='1'`, po reloadu se nezobrazuje ([img/…02-editor-hint.png](img/2026-08-15-m25-02-editor-hint.png), pozn. k plátnu níže) |
| 7 | **Klik-segmentace** (placené $0.005) | ✅ nástroj Segmentace → klik do plátna → busy „Segmentuji prvek na (473, 505)…" → SAM point call → vrstva **„sam: bod (473,505)"** vytvořena, tag SAM, **automaticky vybrána**, status „Vytvořena vrstva … z kliknutí." ([img/…03](img/2026-08-15-m25-03-segment-layer.png), [img/…04](img/2026-08-15-m25-04-canvas-composed.png)) |
| 8 | Rename vrstvy dvojklikem | ✅ dblclick na název → input (autofocus, Enter/Esc/blur) → „figura-schody" → status + **persist v project.json ověřen přes API** + undo snapshot (pytest) |
| 9 | Správa projektů | ✅ na throwaway projektech: ✎ inline rename „junk-drop"→„koš-test"; ⧉ duplicate „junk2"→„junk2 (kopie)" (nové id, bez history.json — pytest); 🗑 delete 3× s potvrzením — zachyceny české zprávy „Smazat projekt „X"? Tuto akci nelze vrátit."; disk po úklidu čistý (4 reálné projekty) |
| 10 | Access log audit | ✅ v logu (`logs/uvicorn-out.log`, zapnut během session) přesně 3 moje DELETE, nic cizího |

Akceptace „nový uživatel bez nápovědy: nahrát dragem → rozložit → kliknout-vysegmentovat → přejmenovat vrstvu → smazat starý projekt" — **splněna** (dekompozice dropem po wiring ověření odložena kvůli platebnímu limitu; zbytek klikáním).

## Nálezy (obecné → LEARNINGS)

1. **SAM-3 point mode: klíč `prompt` VŽDY** — vynechání = server dosadí schema default `"wheel"` → 0 masek. `prompt:""` + bod → maska prvku (part-level, ~30×45 px). Detail `docs/capabilities/sam3-point.md`, fix `ccdf5a8`.
2. **Neošetřená výjimka = 500 bez CORS hlaviček** → frontend vidí jen „Failed to fetch". Uživatelské vstupy (upload) validovat na `HTTPException(4xx)`. Fix `53a880b`.
3. **`create_project` neuniquifikoval adresář** — dvojí drop téhož souboru by tiše přepsal manifest prvního projektu. Fix `0d14ed2`.
4. E2E automatizace (claude-in-chrome, tato instance): ref-kliky nespolehlivé (hlásí úspěch, klik nedorazí) — klikat na souřadnice ze screenshotů (screenshot px = CSS px × 1568/innerWidth) nebo JS `.click()`; background tab po reloadu → ResizeObserver nedoběhne → Konva Stage 1×1 (rAF throttling) — pro exporty nutná ruční korekce rozměrů; html2canvas Konva canvasy nevykreslí (plátno v 02/03 prázdné — kompozice viz 04 = přímý `stage.toDataURL`).

## ⚠️ INCIDENT: ztráta projektu openart

Během session **zmizel z disku projekt `openart-image_1786520032733_….grafik`** (id `9d545b9cde61`, 4 vrstvy, vytvořen 14.8.). Timeline (UTC): 00:16 v API listu ✓ → ~00:22–26 první corrupt-drop + **server ~2 min zaseknutý** (pre-fix éra) + 2× restart API → ~00:28–31 UI ukázalo openart s čerstvým `updated_at` ≈00:24 a současně se objevil banner dekompozice-400, kterou jsem nevyvolal (jiná adresa BytesIO než moje) → 00:33 adresář pryč. **Mechanismus se nepodařilo prokázat** — žádný můj krok DELETE na openart nevolal; access log tehdy neběžel; podezření na replay/misroute requestů kolem zaseknutého serveru a restartů.

**Mitigace provedené:** (a) záloha `projects-backup-<stamp>/` (všechny přeživší projekty), (b) API od té doby běží s access logem (`logs/uvicorn-*.log`) — každý DELETE je evidovaný, (c) audit po zbytku session čistý.

**Obnova:** zdrojový obrázek existuje v `C:\Users\stock\Downloads\openart-image_….png` → re-decompose (~$0.02–0.05) po odsouhlasení.

**Obnoveno (2026-08-15):** re-decompose zdrojáku z Downloads přes API (`POST /api/projects` + `/decompose/file?num_layers=4`, 1 placené volání) → nový projekt id `75778f1bd1a4`, stejný název adresáře jako původní. Ověřeno: figuruje v `GET /api/projects` (4 vrstvy), kompozit HTTP 200; obsah kompozitu vs zdroj mean diff 4,78/255. Pozn.: fal I2L vrátil vrstvy 544×736 na plátně 1792×2400 (auto-set z uploadu) → obsah v levém horním rohu — shodné chování jako kanonický drag&drop flow u velkých obrázků, viz LEARNINGS 2026-08-15. Následně layout všech 4 vrstev škálován na plátno přes `POST /layers/{id}/transform` (1792×2400 @ 0,0) → kompozit pokrývá celé plátno, mean diff vs zdroj 6,08/255 v plném rozlišení; editor projekt načítá (4 vrstvy, bez console errors).

**Kandidáti M4:** soft-delete (koš místo okamžitého rmtree), periodická záloha `projects/`, prošetření wedge chování uvicornu při pomalém multipart uploadu.
